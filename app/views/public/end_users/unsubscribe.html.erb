<div class="container">
  <div class="row my-5 mx-auto border">
    <h2 class="col-md-10 my-5 mx-auto text-center">本当に退会しますか？</h2>

    <div class="col-md-10 mx-auto">
      <p class="row my-3 justify-content-center text-center">
        退会すると、会員登録情報や<br>
        これまでのご自身の投稿が閲覧できなくなります.<br>
        退会する場合は、「退会する」をクリックしてください.
      </p>

      <div class="row my-5 justify-content-center justify-align-center">
        <din class="col-md-auto mt-3 text-center">
          <%= link_to "退会しない", end_user_path(@end_user), class: "btn btn-primary" %>
        </din>
        <%= form_with model: @end_user, url: end_users_withdraw_path, method: :patch, class: "col-md-2 offset-md-1 text-center mt-3" do |f| %>
          <%= f.hidden_field :is_deleted, :value => "true" %>
          <%= f.submit "退会する", class: "btn btn-danger" %>
        <% end %>
      </div>
    </div>
  </div>
</div>