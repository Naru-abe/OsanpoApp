<% if end_user_signed_in? %>
  <div class="container pt-2">
    <div class="row justify-content-between">
      <div class="form-inline m-3">
        <%= form_tag posts_path, method: :get, class: "row" do %>
        <div class="col-auto">
          <%= select_tag :tag_id, options_from_collection_for_select(Tag.all, :id, :name, params[:tag_id]), { prompt: 'タグで絞り込み検索', onchange: 'submit(this.form);', class: "form-control" } %>
        </div>
        <% end %>
      </div>

      <div class="form-inline m-3">
        <%= form_with url: search_path, method: :get, local: true, class: "row" do |f| %>
          <div class="col-auto pr-0">
          <%= f.text_field :content, placeholder: "キーワードで検索", class: "form-control" %>
          </div>
          <div class="col-auto pr-0">
          <%= f.select :model, options_for_select({ "ユーザー" => "end_user", "投稿" => "post" }), {}, {:class => "form-control"}%>
          </div>
          <div class="col-auto">
          <%= f.submit '検索', class: "btn btn-outline-success" %>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
</div>